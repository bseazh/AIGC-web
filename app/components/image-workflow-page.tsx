"use client";

import { ArrowLeft, Check, Download, FolderOpen, ImagePlus, LoaderCircle, Sparkles, Upload, X } from "lucide-react";
import { useRouter } from "next/navigation";
import { FormEvent, useEffect, useState } from "react";

type Props = { title: string; description: string; submitUrl: string; scenes: readonly string[]; styles: readonly string[]; sourceTitle: string; sourceHint: string; submitLabel: string; pointsPerTask?: number; outputCount?: number; showAspectRatio?: boolean; defaultRatio?: string; sceneLabel?: string; styleLabel?: string; nextStepHref?: string; nextStepLabel?: string };
type Account = { wallet: { availablePoints: number } };
type TaskResult = { taskId: string; status: string; outputs: Array<{ assetId: string; url: string }>; errorCode?: string };
type Asset = { id: string; mimeType: string; byteSize: number; originalName: string; url: string; kind: string };
const ratios = ["1:1", "3:4", "4:3", "9:16"];

export function ImageWorkflowPage({ title, description, submitUrl, scenes, styles, sourceTitle, sourceHint, submitLabel, pointsPerTask = 10, outputCount = 4, showAspectRatio = true, defaultRatio = "1:1", sceneLabel = "使用场景", styleLabel = "视觉风格", nextStepHref, nextStepLabel }: Props) {
  const router = useRouter();
  const [account, setAccount] = useState<Account | null>(null); const [file, setFile] = useState<File | null>(null); const [selectedAsset, setSelectedAsset] = useState<Asset | null>(null); const [preview, setPreview] = useState(""); const [assets, setAssets] = useState<Asset[]>([]); const [libraryOpen, setLibraryOpen] = useState(false); const [assetsLoading, setAssetsLoading] = useState(false);
  const [ratio, setRatio] = useState(defaultRatio); const [scene, setScene] = useState(scenes[0]); const [style, setStyle] = useState(styles[0]); const [prompt, setPrompt] = useState(""); const [phase, setPhase] = useState<"idle" | "uploading" | "generating" | "succeeded" | "failed">("idle"); const [error, setError] = useState(""); const [task, setTask] = useState<TaskResult | null>(null);
  useEffect(() => { fetch("/api/auth/session/", { cache: "no-store" }).then(async (response) => { if (!response.ok) throw new Error(); setAccount(await response.json()); }).catch(() => router.replace("/")); }, [router]);
  useEffect(() => { const params = new URLSearchParams(window.location.search); const presetScene = params.get("scene"); const presetStyle = params.get("style"); const presetPrompt = params.get("prompt"); if (presetScene && scenes.includes(presetScene)) setScene(presetScene); if (presetStyle && styles.includes(presetStyle)) setStyle(presetStyle); if (presetPrompt) setPrompt(presetPrompt.slice(0, 1200)); }, [scenes, styles]);
  useEffect(() => () => { if (preview.startsWith("blob:")) URL.revokeObjectURL(preview); }, [preview]);
  useEffect(() => { const id = new URLSearchParams(window.location.search).get("assetId"); if (!account || !id || selectedAsset || file) return; fetch("/api/assets/?kind=ALL", { cache: "no-store" }).then(async (response) => { const body = await response.json(); const asset = (body.assets || []).find((item: Asset) => item.id === id && item.mimeType.startsWith("image/")); if (asset) { setSelectedAsset(asset); setPreview(asset.url); } }).catch(() => undefined); }, [account, file, selectedAsset]);
  const request = async (url: string, init: RequestInit) => { const response = await fetch(url, init); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.message || body.code || "请求失败"); return body; };
  const resetTask = () => { setError(""); setTask(null); setPhase("idle"); };
  const chooseFile = (nextFile?: File) => { if (!nextFile) return; if (!["image/jpeg", "image/png", "image/webp"].includes(nextFile.type)) return setError("仅支持 JPG、PNG、WebP 图片"); if (nextFile.size > 10 * 1024 * 1024) return setError("图片不能超过 10MB"); if (preview.startsWith("blob:")) URL.revokeObjectURL(preview); setSelectedAsset(null); setFile(nextFile); setPreview(URL.createObjectURL(nextFile)); resetTask(); };
  const openLibrary = async () => { setLibraryOpen(true); setAssetsLoading(true); setError(""); try { const response = await fetch("/api/assets/?kind=ALL", { cache: "no-store" }); const body = await response.json(); if (!response.ok) throw new Error(); setAssets((body.assets || []).filter((asset: Asset) => asset.mimeType.startsWith("image/"))); } catch { setError("素材库加载失败，请稍后再试"); } finally { setAssetsLoading(false); } };
  const selectAsset = (asset: Asset) => { if (preview.startsWith("blob:")) URL.revokeObjectURL(preview); setFile(null); setSelectedAsset(asset); setPreview(asset.url); setLibraryOpen(false); resetTask(); };
  const pollTask = async (taskId: string) => { const deadline = Date.now() + 6 * 60 * 1000; while (Date.now() < deadline) { const response = await fetch(`/api/tasks/${taskId}/`, { cache: "no-store" }); const current = await response.json(); if (!response.ok) throw new Error(current.message || "任务查询失败"); setTask(current); if (current.status === "SUCCEEDED") { setPhase("succeeded"); const session = await fetch("/api/auth/session/", { cache: "no-store" }).then((item) => item.json()); setAccount(session); return; } if (["FAILED", "REJECTED", "CANCELED"].includes(current.status)) throw new Error(current.errorCode || "生成失败，积分已退回"); await new Promise((resolve) => setTimeout(resolve, 3000)); } throw new Error("任务等待超时，请稍后在任务中心查看"); };
  const submit = async (event: FormEvent) => { event.preventDefault(); if ((!file && !selectedAsset) || ["uploading", "generating"].includes(phase)) return; setError(""); setPhase("uploading"); try { let assetId = selectedAsset?.id; if (file) { const presign = await request("/api/uploads/presign/", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ fileName: file.name, mimeType: file.type, byteSize: file.size }) }); const upload = await fetch(presign.uploadUrl, { method: "PUT", headers: { "Content-Type": file.type }, body: file }); if (!upload.ok) throw new Error(`素材上传失败 (${upload.status})`); await request("/api/uploads/confirm/", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ assetId: presign.assetId }) }); assetId = presign.assetId; } const created = await request(submitUrl, { method: "POST", headers: { "Content-Type": "application/json", "Idempotency-Key": crypto.randomUUID() }, body: JSON.stringify({ assetId, prompt, aspectRatio: ratio, scene, style }) }); setPhase("generating"); await pollTask(created.taskId); } catch (caught) { setError(caught instanceof Error ? caught.message : "生成失败"); setPhase("failed"); } };
  if (!account) return <main className="workspace-loading"><span><Sparkles size={22} /></span><p>正在载入芭乐AIGC</p></main>;
  const busy = phase === "uploading" || phase === "generating";
  return <main className="creator-shell"><header className="creator-header"><button className="icon-button" aria-label="返回工作台" onClick={() => router.push("/workspace")}><ArrowLeft size={19} /></button><div><strong>{title}</strong><span>芭乐AIGC</span></div><div className="creator-points"><Sparkles size={15} />{account.wallet.availablePoints} 积分</div></header><form className="creator-layout" onSubmit={submit}><section className="upload-stage">{!preview ? <div className="image-source-choice"><label className="upload-drop"><span><ImagePlus size={28} /></span><strong>{sourceTitle}</strong><small>{sourceHint}</small><input type="file" accept="image/jpeg,image/png,image/webp" onChange={(event) => chooseFile(event.target.files?.[0])} /></label><button type="button" className="image-library-button" onClick={openLibrary}><FolderOpen size={17} />从内容资产选择</button></div> : <div className="source-preview"><img src={preview} alt="待生成商品素材" /><div className="source-preview-actions">{selectedAsset && <span>已引用资产素材</span>}<button type="button" className="icon-button" aria-label="移除图片" onClick={() => { if (preview.startsWith("blob:")) URL.revokeObjectURL(preview); setFile(null); setSelectedAsset(null); setPreview(""); resetTask(); }}><X size={18} /></button></div></div>}{busy && <div className="generation-overlay"><LoaderCircle size={30} /><strong>{phase === "uploading" ? "正在上传并校验素材" : `正在生成 ${outputCount} 张${title}`}</strong><span>任务由服务器异步处理</span></div>}{phase === "succeeded" && task && <div className="result-grid">{task.outputs.map((output, index) => <article key={output.assetId}><img src={output.url} alt={`${title}结果 ${index + 1}`} /><a href={output.url} download target="_blank" rel="noreferrer"><Download size={16} />下载</a>{nextStepHref && <button type="button" className="result-next" onClick={() => router.push(`${nextStepHref}?assetId=${output.assetId}`)}>{nextStepLabel || "继续创作"}</button>}</article>)}</div>}</section><aside className="creator-panel"><div className="panel-title"><span><Sparkles size={18} /></span><div><h1>生成设置</h1><p>{description}</p></div></div>{showAspectRatio && <><label className="field-label">画幅比例</label><div className="ratio-control">{ratios.map((item) => <button type="button" key={item} className={ratio === item ? "active" : ""} onClick={() => setRatio(item)}>{item}</button>)}</div></>}<label className="field-label">{sceneLabel}<select value={scene} onChange={(event) => setScene(event.target.value)}>{scenes.map((item) => <option key={item}>{item}</option>)}</select></label><label className="field-label">{styleLabel}<select value={style} onChange={(event) => setStyle(event.target.value)}>{styles.map((item) => <option key={item}>{item}</option>)}</select></label><label className="field-label">补充要求<textarea value={prompt} onChange={(event) => setPrompt(event.target.value)} maxLength={1200} placeholder="例如：商品放在窗边桌面，保留自然投影与留白" /><small>{prompt.length}/1200</small></label>{error && <p className="creator-error" role="alert">{error}</p>}{phase === "succeeded" && <p className="creator-success"><Check size={16} />{outputCount} 张结果已保存到内容资产</p>}<button className="generate-button" type="submit" disabled={(!file && !selectedAsset) || busy || account.wallet.availablePoints < pointsPerTask}><Upload size={18} />{busy ? "任务处理中" : account.wallet.availablePoints < pointsPerTask ? "积分不足" : submitLabel}</button></aside></form>{libraryOpen && <div className="asset-picker-backdrop" role="dialog" aria-modal="true" aria-label="选择图片素材"><section className="asset-picker-modal"><header><div><span>内容资产</span><h2>选择图片素材</h2></div><button type="button" className="icon-button" onClick={() => setLibraryOpen(false)}><X size={18} /></button></header>{assetsLoading ? <div className="asset-picker-empty"><LoaderCircle size={22} />正在加载素材</div> : assets.length ? <div className="asset-picker-grid">{assets.map((asset) => <button type="button" key={asset.id} onClick={() => selectAsset(asset)}><img src={asset.url} alt="" /><strong>{asset.originalName}</strong><small>{asset.kind === "OUTPUT" ? "生成结果" : "上传素材"}</small></button>)}</div> : <div className="asset-picker-empty"><FolderOpen size={25} /><strong>暂无图片素材</strong><p>上传或完成一次生成后，素材会自动显示在这里。</p></div>}</section></div>}</main>;
}
